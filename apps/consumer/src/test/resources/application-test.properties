# Test configuration for Consumer application
spring.profiles.active=test
logging.level.root=WARN
logging.level.br.com.itau=DEBUG

# Test AWS configuration
AWS_REGION=us-east-1

# Test Kafka configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=test-consumer-group
app.kafka.topics.transactions=test-transactions

# Test DynamoDB configuration
app.dynamodb.transactions-table=test-transactions-table

# Test metrics configuration
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=always
